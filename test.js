(async () => {
  try {
    const targetEl = document.getElementById('servers-list');
    if (!targetEl) return;
    // Code is working correctly
  } catch (error) {
    console.error('Error:', error);
  }
})();